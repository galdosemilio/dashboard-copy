import { SectionConfigDetails } from './section.config'

export const BeSlimSectionConfig: SectionConfigDetails = {
  CHECKOUT: {
    AUTOMATIC_SHOPIFY_REDIRECT: true
  },
  LOGIN: {
    SHOW_REGISTER_NEW_COMPANY: false,
    USE_COOKIE_BASED_SESSION: true
  }
}
