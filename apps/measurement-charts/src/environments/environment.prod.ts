import { Environment } from './environment.interface'

export const environment: Environment = {
  apiUrl: 'https://api.coachcare.com/',
  appName: 'ccr-measurement-chart',
  production: true,
  role: 'provider',
  selveraApiEnv: 'prod'
}
