import { SidenavOptions } from '../consts'
import { SectionConfigDetails } from '../models/section.details'

export const ApolloUSClinicsProdSectionConfig: SectionConfigDetails = {
  SIDENAV: {
    SHOWN_OPTIONS: [SidenavOptions.REPORT_CUSTOM]
  }
}

export const ApolloUSClinicsTestSectionConfig: SectionConfigDetails = {
  SIDENAV: {
    SHOWN_OPTIONS: [SidenavOptions.REPORT_CUSTOM]
  }
}
