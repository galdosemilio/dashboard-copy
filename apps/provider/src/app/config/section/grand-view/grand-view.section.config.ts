import { _ } from '@app/shared/utils'
import { SectionConfigDetails } from '../models/section.details'

export const GrandViewHealthSectionConfig: SectionConfigDetails = {
  RPM: {
    ALLOW_NO_DEVICE_SELECTION: true
  }
}
