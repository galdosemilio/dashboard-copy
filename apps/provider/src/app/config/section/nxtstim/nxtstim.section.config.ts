import { MeasurementTabs } from '../consts'
import { SectionConfigDetails } from '../models/section.details'

export const NXTSTIMSectionConfig: SectionConfigDetails = {
  JOURNAL: {
    HIDDEN_MEASUREMENT_TABS: [MeasurementTabs.FOOD]
  }
}
