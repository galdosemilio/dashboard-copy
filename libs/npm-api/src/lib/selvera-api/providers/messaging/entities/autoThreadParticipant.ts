/**
 * Org Auto Thread Participant
 */

export interface AutoThreadParticipant {
  /** The id of the account. */
  id: string
  /** The first name of the account. */
  firstName: string
  /** The last name of the account. */
  lastName: string
  /** The time the preference was created (in UTC) */
  createdAt: string
}
