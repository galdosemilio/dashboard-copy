/**
 * FoodMealConsumption
 */

export interface FoodMealConsumption {
  /** Number of times the meal was consumed. */
  count: number
  /** A percentage (rounded to 2 decimal places) which indicates what part of total meal consumption count this meal is. */
  percentage: number
}
