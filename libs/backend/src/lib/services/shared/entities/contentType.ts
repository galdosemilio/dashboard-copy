/**
 * ContentType
 */

export interface ContentType {
  /** Item type ID. */
  id: number;
  /** Unique item type code. */
  code: string;
  /** Extended item type description. */
  description: string;
}
