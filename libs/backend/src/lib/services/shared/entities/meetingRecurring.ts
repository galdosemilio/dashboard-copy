/**
 * MeetingRecurring
 */

export interface MeetingRecurring {
  /** Recurring template ID. */
  recurringTemplateId: string;
  /** A flag indicating whether a single meeting from the recurring series was edited. */
  edited: boolean;
}
